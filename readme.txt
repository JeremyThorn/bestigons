This will be a lovely set of hexmen.

We will have structs for all inputs to classes.

Liquid and obstacle will be structs that are cell attributes.

Have a global structs, classes, and typedefs header file.

Uppercase starting letter for classes, structs. lowercase everything else. _ for spaces.
